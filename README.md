CarCollect Bot

«CarCollect» представляет собой программное решение (далее – «Комплекс»), реализованное на языке программирования Python с применением асинхронного фреймворка aiogram 3.x. Комплекс предоставляет конечным пользователям функционал для осуществления коллекционирования виртуализированных объектов (карточек автомобилей). Предусмотрены механики приобретения означенных объектов посредством специализированных контейнеров («кейсов»), проведения бартерных операций с другими участниками системы, синтеза («крафта») новых объектов из дублирующихся экземпляров, а также участия в интегрированных мини-играх.

🚀 Функциональные возможности

Агрегирование коллекции: Реализован обширный интерфейс «гаража», предоставляющий возможности для просмотра, сортировки и фильтрации парка виртуальных автомобилей.

Система контейнеров («Кейсы»): Предусмотрено получение объектов из бесплатных контейнеров с настраиваемым периодом восстановления (cooldown).

Экономическая модель: Введена система, базирующаяся на двух сущностях: «Покрышки» (🛞) в качестве основной внутриигровой расчетной единицы и «CR» в качестве меры оценки совокупной стоимости коллекции.

Интерфейс коммерческих операций («Магазин»): Интегрированный модуль для приобретения дополнительных попыток открытия контейнеров, а также «Покрышек» (посредством валюты Telegram Stars ⭐).

Система подписки «CollectPass»: Реализована опциональная месячная подписка, предоставляющая пользователям определенные преимущества (например, сокращенные периоды восстановления, дисконты, расширенные лимиты операций).

Механизм синтеза («Крафт»): Система, позволяющая осуществлять обмен дублирующихся экземпляров автомобилей на один объект более высокой категории редкости.

Протокол бартерных операций («Обмен»): Полнофункциональный механизм безопасного, подтверждаемого обмена объектами между двумя участниками системы.

Интегрированные игровые модули («Мини-игры»): Включают модуль «Кинуть кубик» (для приобретения доп. попыток) и «Бросить монетку» (для приобретения «Покрышек»).

Управление учетной записью («Профиль»): Функционал для администрирования псевдонима пользователя, отслеживания реферальной программы и активации промокодов.

Функционал взаимодействия с групповыми чатами: Включает систему Airdrops (случайное распределение объектов в чате) и генерацию рейтинговых таблиц (/leaderboard).

Панель администрирования: Предусмотрен расширенный интерфейс для администрирования Комплекса (начисление ресурсов, сбор статистики, управление промокодами, резервное копирование, техническая поддержка).

🛠️ Технологический стек

Python 3.10+

aiogram 3.x (Асинхронный фреймворк для взаимодействия с Telegram Bot API)

PostgreSQL (Система управления базами данных)

psycopg2-binary (Драйвер для СУБД PostgreSQL)

python-dotenv (Модуль для управления переменными окружения)

⚙️ Развертывание и ввод в эксплуатацию

1. Клонирование репозитория исходного кода

git clone [https://github.com/ВАШ_ЛОГИН/ВАШ_РЕПОЗИТОРИЙ.git](https://github.com/ВАШ_ЛОГИН/ВАШ_РЕПОЗИТОРИЙ.git)
cd ВАШ_РЕПОЗИТОРИЙ



2. Конфигурирование виртуальной среды и установка зависимостей

# Создание виртуальной среды
python3 -m venv venv

# Активация виртуальной среды
source venv/bin/activate

# Установка требуемых библиотек
pip install -r requirements.txt



3. Конфигурирование базы данных PostgreSQL

Функционирование Комплекса предполагает наличие настроенной СУБД PostgreSQL.

Необходимо осуществить установку PostgreSQL на сервер.

Требуется подключение к psql с правами суперпользователя (например, sudo -u postgres psql).

Необходимо осуществить создание пользователя и базы данных:

-- 1. Создание пользователя (замените 'ВАШ_ПАРОЛЬ' на надежный пароль)
CREATE USER ваш_пользователь_бд WITH PASSWORD 'ВАШ_ПАРОЛЬ';

-- 2. Создание базы данных
CREATE DATABASE carbot_db;

-- 3. Назначение созданного пользователя владельцем базы данных (обязательный шаг)
ALTER DATABASE carbot_db OWNER TO ваш_пользователь_бд;

-- 4. Выход из psql
\q



4. Параметризация конфигурации

Требуется создание файла .env в корневом каталоге проекта. Следует скопировать содержимое example.env (при наличии) или создать файл со следующим содержимым:

# .env

# Токен, полученный от @BotFather
MAINTOKEN="123456:ABC-DEF1234567890"

# Имя пользователя базы данных (созданного на шаге 3)
SERVERUSERNAME="ваш_пользователь_бд"

# Пароль для указанного пользователя
SERVERPASSWORD="ВАШ_ПАРОЛЬ"



Подлежит редактированию файл config.py:

Проверке подлежит блок DB_CONFIG. Параметры dbname (carbot_db), user и password будут загружены из .env. Параметры host и port по умолчанию (localhost, 5432).

Обязательной параметризации подлежат ADMIN_IDS (Telegram ID администратора) и CHANNEL_ID (ID канала для верификации подписки).

Параметр TESTER_IDS заполняется опционально.

5. Первичная инициализация

В ходе первичной инициализации Комплекс автоматически выполнит создание всех требуемых таблиц в базе данных.

# Требуется убедиться, что виртуальная среда (venv) активна
python3 main.py



6. Ввод в эксплуатацию (Production)

В целях обеспечения стабильной работы на сервере рекомендуется использовать nohup с явным указанием полного пути к интерпретатору Python из виртуальной среды:

# Замените /home/smalllbro/carc на ВАШ ПУТЬ к каталогу проекта
nohup /home/smalllbro/carc/venv/bin/python3 main.py &



🔐 Инструменты администрирования

(Доступ предоставляется исключительно пользователям, чьи идентификаторы перечислены в ADMIN_IDS файла config.py)

/give [USER_ID] [type] [amount] — Начисление ресурса.

Типы: tires, extra_attempts.

Пример: /give 12345 tires 100

/give [USER_ID] car "Название машины" [amount] — Начисление объекта «автомобиль» (в единичном или множественном экземпляре).

Пример 1: /give 12345 car "Ford Focus"

Пример 2: /give 12345 car "Audi A4" 3

/addpromo [CODE] [type] [value] — Инициация нового промокода.

Пример: /addpromo WELCOME extra_attempts 10

/addpromo [CODE] car "Название машины" — Инициация промокода на объект «автомобиль».

/check [USER_ID] — Аудит учетной записи пользователя (история балансов, транзакций).

/stats — Отображение агрегированных статистических данных Комплекса.

/tickets — Отображение перечня активных обращений в службу поддержки.

/backup — Инициация процесса резервного копирования базы данных (результат сохраняется в каталог backups/).

/broadcast [TEXT] — Выполнение массовой рассылки уведомлений пользователям.

/ban [USER_ID] / /unban [USER_ID] — Управление статусами блокировки пользователей.

👥 Интеграция с групповыми чатами

/enable_airdrops — Активация функции Airdrops (случайное распределение объектов) в текущем чате.

/disable_airdrops — Деактивация функции Airdrops.

/leaderboard — Отображение рейтинга (топ-10) участников чата по совокупной стоимости коллекции.

📄 Условия лицензирования

Настоящий программный проект распространяется на условиях лицензии GNU General Public License v3.0 (GPL-3.0).

Вышеупомянутая лицензия подразумевает предоставление права на:

Эксплуатацию данного программного кода в любых целях.

Внесение модификаций в данный программный код.

Распространение модифицированных версий.

Одновременно с этим, на лицензиата налагаются следующие обязательства:

Обеспечение доступа к исходному коду любых производных работ, подлежащих публикации.

Применение аналогичной лицензии (GPLv3) для производных работ.

Дополнительная информация о лицензионном соглашении доступна по адресу: https://www.gnu.org/licenses/gpl-3.0.html
